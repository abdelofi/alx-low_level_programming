#!/bin/bash

# Get the C file name from the environment variable
CFILE="$CFILE"

# Check if the C file exists
if [ ! -f "$CFILE" ]; then
  echo "Error: C file $CFILE not found"
  exit 1
fi

# Run the preprocessor and save the result to c
gcc -E "$CFILE" -o c
